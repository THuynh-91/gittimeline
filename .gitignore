node_modules
dist
dist-*
dist-ssr
*.local
.DS_Store
playwright-report
test-results
blob-report
coverage
.vite
debug-out.txt

# Pre-fetched histories: a build product, rebuilt by CI on every deploy.
public/catalog/
.catalog-build/
.catalog-release/

# Soundtrack: fetched at build time, like the catalog.
public/music/

# Scratch harnesses and measurement output. These are written next to the
# source so `playwright` resolves from node_modules, and they are not the
# project — a build directory and a dozen one-off probes once went into a
# commit because nothing here said not to.
.measure-dist/
gt-*.mjs
gt-*.log
gt-measure-results.json
gt-*.ts
scripts/_measure-server.mjs

# One-off probe scripts and measurement harnesses, same reason as above.
x/
